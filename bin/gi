#!/bin/sh
set -e

VERSION=lates
DEFAULT_DISTRIBUTION=${DB_DISTRIBUTION:=t}

IMAGE_NAME=galileo/git \
IMAGE_TAG_VERSION=${VERSION}${DEFAULT_DISTRIBUTION} \
COMMAND_TO_EXECUTE=git \
.docker_binary_execute "$@"
